service: default
runtime: python38

basic_scaling:
    max_instances: 25 
    idle_timeout: 10m

resources:
    cpu: 1
    memory_gb: 4
    disk_size_gb: 10

default_expiration: "1m"

entrypoint: gunicorn -b 0.0.0.0:8080 main:server

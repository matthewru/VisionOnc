service: default
runtime: python312

basic_scaling:
    max_instances: 25 
    idle_timeout: 10m

resources:
    cpu: 1
    memory_gb: 4
    disk_size_gb: 10

default_expiration: "1m"

entrypoint: gunicorn -b :$PORT main:server
